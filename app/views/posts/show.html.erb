<div class="bbs-screen bbs-content">
  <div class="article-metaline">
    <span class="article-meta-tag">作者</span>
    <span class="article-meta-value"><%= @post.author %></span>
  </div>
  <div class="article-metaline-right">
    <span class="article-meta-tag">看板</span>
    <span class="article-meta-value">PTTrend</span>
  </div>
  <div class="article-metaline">
    <span class="article-meta-tag">標題</span>
    <span class="article-meta-value"><%= @post.title %></span>
  </div>
  <div class="article-metaline">
    <span class="article-meta-tag">時間</span>
    <span class="article-meta-value"><%= @post.updated_at.strftime("%a %b %d %H:%M:%S %Y") %></span>
  </div>
  <p>
    <%= simple_format @post.content %>
  </p>
  <div class="r-list-sep"></div>
  <span class="f2">
    ※ 政治文機率：<%= @post.proba_of_political %>%
    <br/>
    ※ 非政治文機率：<%= 100 - @post.proba_of_political %>%
    <br/>
    ※ 得到推噓數：<%= comment_details(@post.comment) %>
  </span>
</div>
